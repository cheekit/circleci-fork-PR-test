version: 2
jobs:
  build:
    docker:
      - image: circleci/node:latest
    steps:
      - checkout
      - run: echo $PROJECT_ENV
  deploy:
    docker:
      - image: circleci/node:latest
    steps:
      - checkout
      - run: echo $CONTEXT_ENV
      
workflows:
  version: 2
  workflow:
   jobs:
     - build
     - hold:
         type: approval
         requires:
           - build
     - deploy:
          context:
             - forked-project

